import styled from 'styled-components';

const InputWrapper = styled.h2`
  color: #75736d;
  margin-top: 0px;
  font-size: 24px;

  @media (max-width: 420px) {
    font-size: 16px;
  }
`;

export default InputWrapper;
